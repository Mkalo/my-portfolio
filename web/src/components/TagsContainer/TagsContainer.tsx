const TagsContainer = () => {
  return (
    <div>
      <h2>{'TagsContainer'}</h2>
      <p>{'Find me in ./web/src/components/TagsContainer/TagsContainer.tsx'}</p>
    </div>
  );
};

export default TagsContainer;
